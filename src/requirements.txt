# Core ML
scikit-learn>=1.5.0
pandas>=2.2.2
pyarrow>=17.0.0

# MLflow tracking & model registry
mlflow>=2.15.1

# MLflow PostgreSQL backend store driver
psycopg2-binary>=2.9.9

# --- Agent & Graph Orchestration ---
langchain-google-genai    # Gemini API wrapper
langgraph                 # Agent workflow orchestration
langgraph-checkpoint-postgres  # PostgreSQL checkpointer for graph state
psycopg[binary]           # Required by langgraph-checkpoint-postgres

# --- Vector Database & RAG ---
langchain                 # Core library for RAG components
pgvector                  # Python bindings for PostgreSQL vector embeddings

# --- Web Backend ---
fastapi                   # Web framework for the agent API
uvicorn[standard]         # ASGI server to run FastAPI